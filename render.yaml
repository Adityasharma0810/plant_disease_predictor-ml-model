services:
  - type: web
    name: crop-disease-api-2
    env: python
    rootDir: backend2
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn modelapiuse:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13